
-----------------------------------------------------------------------------------------------------------------------------------------------------------

$ : wget https://storage.googleapis.com/flutter_infra_release/releases/stable/linux/flutter_linux_3.7.7-stable.tar.xz

$ : tar -xvf flutter_linux_3.7.7-stable.tar.xz

$ : echo 'export PATH="$PATH:$HOME/Repositorio/flutter/bin"' >> .bashrc           # Añadiendo el directorio bin a las variables de entorno

$ : source .bashrc                                                                # Actualizando las variables de entorno

-----------------------------------------------------------------------------------------------------------------------------------------------------------

Link : https://developer.android.com/studio#downloads

$ : tar -xf <android-studio...>

$ : cd android-studio/bin

$ : ./studio.sh

// Botones : (ANDROID SDK) ---> (SDK TOOLS) ---> (ANDORID SDK LINEA COMANDOS TOOLS)

$ : sudo apt update && sudo apt install android-sdk


$ : flutter emulators --create [--name xyz]

$ : flutter emulators

$ : flutter emulators --launch <emulator_id>


// En otra terminal del mismo proyecto ejecutamos

$ : flutter run

-----------------------------------------------------------------------------------------------------------------------------------------------------------

